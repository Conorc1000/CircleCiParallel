machine:
  pre:
    - sudo apt-get update; sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-4.9 10
  node:
    version: 6.0.0

general:
  artifacts:
    - "test"

test:
  override:
    - case $CIRCLE_NODE_INDEX in 0) npm t ;; 1) npm test2 ;; esac:
      parallel: true