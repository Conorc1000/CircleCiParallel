machine:
  pre:
    - sudo apt-get update; sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-4.9 10
  node:
    version: 6.0.0

general:
  artifacts:
    - "test"

test:
  override:
    - bundle exec rspec:
      parallel: true
      files:
        - npm run test    # can be a direct path to file or npm command
        - npm run test2