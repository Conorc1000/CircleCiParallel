machine:
  pre:
    - sudo apt-get update; sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-4.9 10
  node:
    version: 6.0.0

general:
  artifacts:
    - "test"

test:
  override:
    - npm run test
      parallel: true
    - npm run test2
      parallel: true
